import { StyleSheet } from "react-native";

export default StyleSheet.create({
    pagination: {
        flexDirection: "row",
        justifyContent: "center",
        alignItems: "center",
    },
    circle: {
        width: 6,
        height: 6,
        borderRadius: 3,
        marginHorizontal: 3,
    },
});
