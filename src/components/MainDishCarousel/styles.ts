import { StyleSheet } from "react-native";

export default StyleSheet.create({
    pagination: {
        flexDirection: "row",
        justifyContent: "center",
        alignItems: "center",
        gap: 5,
    },
    circle: {
        width: 6,
        height: 6,
        borderRadius: 3,
    },
});
